# features

Feature modules that compose components and hooks into a slice of the app.

- `matches/MatchPredictionPanel` — client component that loads a match's ensemble
  prediction via TanStack Query and renders the outcome, scorelines, explainability
  and per-model breakdown, with loading and error states.
